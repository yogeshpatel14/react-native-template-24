import {StyleSheet} from 'react-native';
import {colorPlatter} from '../../themes/color-platter';

const SplashStyles = StyleSheet.create({
  mainView: {
    backgroundColor: colorPlatter.black,
    width: '100%',
    height: '100%',
  },
});

export default SplashStyles;
